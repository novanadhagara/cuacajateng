<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.habib.cuaca">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_logo"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_logo_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:windowSoftInputMode="adjustPan" android:name=".MainActivity">
        </activity>
        <activity
                android:name=".infografis.DetailInfoActivity"
                android:configChanges="orientation|keyboardHidden|screenSize"
                android:label="@string/title_activity_detail_info"
                android:theme="@style/FullscreenTheme">
        </activity>
        <activity android:name=".cuaca_besok.DetailTomorrowActivity">
        </activity>
        <activity android:name=".cuaca_terkini.DetailTodayActivity">
        </activity>
        <activity
                android:name=".beranda.DetailFullActivity"
                android:configChanges="orientation|keyboardHidden|screenSize"
                android:label="@string/title_activity_detail_full"
                android:theme="@style/FullscreenTheme">
        </activity>
        <activity android:name=".beranda.DetailActivity">
        </activity>
        <activity
                android:name=".SplashActivity"
                android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>